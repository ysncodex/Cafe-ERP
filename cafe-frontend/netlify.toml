# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: use public/_redirects (copied to dist) so Netlify + Cloudflare Pages stay aligned.
